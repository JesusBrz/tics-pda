<% if notice.present? %>
  <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
<% end %>

<h1 class="sm:justify-center font-bold text-4xl mb-4">Impresoras</h1>
<%= link_to 'Agregar una nueva impresora', new_printer_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
<div class="grid md:grid-cols-2">
  <%= render @printers %>
</div>
